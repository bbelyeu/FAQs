CakePhp 2.x Plugin for storing & displaying faq info for a business website

Setup:
1. Import faqs.sql in Config/Schema/

2. Add the following line to your app/Config/bootstrap.php
    CakePlugin::load('FAQ', array('routes' => true));

3. Use /admin/faqs to manage
